# MongoDB Atlas Configuration
# Replace with your MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/msme-lender-platform?retryWrites=true&w=majority

# Development fallback (local MongoDB)
# MONGODB_URI=mongodb://localhost:27017/msme-lender-platform

# Server Configuration
PORT=5000
NODE_ENV=development

# Add your MongoDB Atlas credentials here:
# 1. Go to MongoDB Atlas (https://cloud.mongodb.com/)
# 2. Create a cluster if you haven't already
# 3. Click "Connect" -> "Connect your application"
# 4. Copy the connection string and replace the placeholder above
# 5. Replace <username> and <password> with your database user credentials
# 6. Replace <cluster> with your cluster name
